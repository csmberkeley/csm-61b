In Java, an \define{iterator} is an object which allows us to traverse a data structure in linear fashion. Every iterator has two methods: \texttt{hasNext} and \texttt{next}.

\begin{lstlisting}
interface Iterator {
    boolean hasNext();
    int next();
}
\end{lstlisting}
